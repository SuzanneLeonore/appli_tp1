name: appli_tp1
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
    assets:
    - assets/catalogue.sql

  http: ^1.1.0
  graphql_flutter: ^5.1.2
  sqflite: ^2.3.0  
  path: ^1.9.0
  mysql_client: ^0.0.27
  english_words: ^4.0.0
  provider: ^6.0.0
  responsive_builder: ^0.7.0
  shelf: ^1.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true